package dev.echoping.betteresurfing.ui.theme

import androidx.compose.material3.Typography

/**
 * MD3 Type Scale：平台默认字体（含中文回退），全部走库默认（§1「MD3 Type Scale 默认」）。
 * v0.3–0.5 曾把 displaySmall/headlineSmall/titleLarge 等压 Medium「靠字号分层」，
 * v0.6.1 回退：标题字重交回规范默认，不再显式覆盖。
 */
val AppTypography = Typography()
